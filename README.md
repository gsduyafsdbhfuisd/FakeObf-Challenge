# FakeObf-Challenge
python obfuscation challenge

AST transformer divided into 2 classes whose logic is hidden in custom bytcode that is executed in a custom VM

no flag, no secrets

you need recreate the encryption algorithm and rebuild the transformers as accurate as possible

after that send the results to @MeowCode1 on Telegram

leaderboard = {
    None:None,

    None:None,

    None:None,

    None:None,
}
